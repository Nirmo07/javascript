import React from 'react';

// Greeting component that accepts a name prop
const Greeting = ({ name }) => {
    return <h1>Hello, {name}!</h1> // Render the greeting message
}

export default Greeting // Export the component
